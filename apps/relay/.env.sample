# JARVIS Relay Configuration
# Copy this file to .env and configure as needed

# ════════════════════════════════════════════════════════════
# MOCK MODES (for local development without API keys)
# ════════════════════════════════════════════════════════════

# LLM Mock Mode - uses deterministic JARVIS-style responses
# When true, no OPENAI_API_KEY or GEMINI_API_KEY needed
LLM_MOCK_MODE=false

# TTS Mock Mode - skips actual TTS API calls
TTS_MOCK_MODE=false

# STT Mock Mode - returns deterministic transcripts
# When true, no OPENAI_API_KEY needed for speech-to-text
STT_MOCK_MODE=false

# ════════════════════════════════════════════════════════════
# LLM CONFIGURATION
# ════════════════════════════════════════════════════════════

# OpenAI API Key (primary LLM provider)
# OPENAI_API_KEY=sk-...

# Google Gemini API Key (fallback LLM provider)
# GEMINI_API_KEY=...

# LLM Model override (default: gpt-4o)
# LLM_MODEL=gpt-4o

# Max tokens for LLM responses (default: 1024)
# LLM_MAX_TOKENS=1024

# LLM temperature (default: 0.7)
# LLM_TEMPERATURE=0.7

# ════════════════════════════════════════════════════════════
# TTS CONFIGURATION
# ════════════════════════════════════════════════════════════

# TTS Provider (cartesia, elevenlabs, or fish)
# TTS_PROVIDER=cartesia

# Provider API Keys (set the ones you use)
# CARTESIA_API_KEY=...
# ELEVENLABS_API_KEY=...
# FISH_AUDIO_API_KEY=...

# Voice IDs
# CARTESIA_VOICE_ID=...
# ELEVENLABS_VOICE_ID=...

# ════════════════════════════════════════════════════════════
# DATABASE
# ════════════════════════════════════════════════════════════

# PostgreSQL connection URL
# DATABASE_URL=postgresql://localhost:5432/jarvis_dev

# ════════════════════════════════════════════════════════════
# SERVER
# ════════════════════════════════════════════════════════════

# Server port (default: 8080)
PORT=8080

# Log level (default: info)
LOG_LEVEL=info
